# =====================================================================
# CONGO ENTERPRISE — GO MODULES CONFIGURATION (THEME ENGINE)
# =====================================================================

# Прямий імпорт теми Congo v2 як ізольованого модуля
[[imports]]
  path = "github.com/jpanther/congo/v2"

# Додаткові налаштування безпеки для модулів (якщо знадобиться розширення)
[hugo]
  [hugo.module]
    noMounts = false
    